<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.AppConfig"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InventarioFerreteriaZamora"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.LoginActivity" />

        <activity android:name=".activities.MainActivity" />

        <activity android:name=".activities.NuevoProductoActivity" />
        <activity android:name=".activities.EditarProductoActivity" />
        <activity android:name=".activities.ListaProductoActivity" />
        <activity android:name=".activities.ListaFirebaseActivity" />

        <activity android:name=".activities.ListaCategoriaActivity" />
        <activity android:name=".activities.NuevaCategoriaActivity" />
        <activity android:name=".activities.EditarCategoriaActivity" />

        <activity android:name=".activities.ListaProveedorActivity" />
        <activity android:name=".activities.NuevoProveedorActivity" />
        <activity android:name=".activities.EditarProveedorActivity" />

    </application>

</manifest>
